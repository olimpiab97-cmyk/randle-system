# Package index — baseline capture remains withheld

The R4 package consists only of draft specifications, closed schemas, immutable authority policies, verification code, fixtures, expectations, governed results, traceability, impact assessment, canonical delta, and remediation reporting.

Authority code: `governed_file_access_DRAFT.py`, `r3_authority_verifier_DRAFT.py` (R4 enforcing surface retained at its historical draft path), `comparison_engine_DRAFT.py`, `schema_validation_DRAFT.py`, `historical_log_parser_DRAFT.py`, `selection_engine_DRAFT.py`, `inventory_generator_DRAFT.py`, `boundary_verifier_DRAFT.py`, `fixture_runner_DRAFT.py`, and `fixture_runner_R4_DRAFT.py`.

Authority objects and every schema are enumerated by `r4_authority_bindings_DRAFT.json`. Static expectations are never generated by the runner. Actual observations are never used to author expectations.

Operational capture-script work is not authorized. Baseline capture is not authorized. Merge remains withheld. Deployment remains withheld.
