# Canonical Delta — proposal only

Nothing in this file is incorporated into canonical authority. Each row identifies a possible later amendment only after independent acceptance and separately authorized incorporation.

| Canonical area | Proposed later amendment |
|---|---|
| Requirement registry | Require exact commit/blob/path/range/clause-byte/hash provenance and positive, rejection, evidence, effect, durability, retry/restart, and reconciliation obligations. |
| Case authority | Permit only independently authored cases mapped to exact requirement records; prohibit discarded objects, invented clauses, summaries, and case-to-case normative inheritance. |
| Expectation authority | Establish a separately authored, pre-execution, content-addressed oracle with schema/semantic checks and OS ACL separation from execution/observation. |
| Governed scripts | Register every derivation, build, staging, installation-plan, upgrade-plan, matrix, evidence, trace, verification, scanning, and host-capture script by path, Git blob, raw SHA-256, mode, size, stage, dependency, and authority class. |
| External utilities | Require absolute-path, held-measurement, content/identity, configuration, recursive-root, command allowlist, stage, and runtime-authority classification for Git, PowerShell, compiler/IL/framework, SCM/ACL/filesystem, one-time public-file owner recovery, jobs, and modules. |
| Source and role registry | Reverse-map every source to requirement/blocker, architecture role, verification, authority class, installed consumer, and explicit executable source set. |
| Principal registry | Register the five terminal signer, execution, observation, comparator, and upgrade identities; restricted SID; privilege allowlist; LSA logon denials; pipe, filesystem, key, trust, and repository rights. |
| Terminal signer | Treat children as raw-evidence producers only and independently resolve caller/process/token/frame/file/ledger/receipt/effect/version/transaction/recovery/trace semantics. Prohibit signer-token semantic children. |
| Upgrade authority | Establish a separate restricted principal, RSA-3072 nonexportable key, public trust, fixed pipe/policy/ledger, signed provisioning attestation, policy-constructed one-time pre-install authorization, current-state capture plus consumption-time remeasurement, anti-downgrade state, public verification, and separately governed future rollback/revocation. Expose no generic signing, terminal receipt, reconciliation, installation, or activation operation. |
| Trust/version registry | Resolve every service/policy/interface/schema/key version by effective sequence while preserving historic trust and publishing revocation/supersession transitions. |
| Receipt schemas | Define terminal proposal/candidate/fresh/reconciliation, upgrade, recovery, abort, supersession, and historical-classification envelopes and public classes. |
| Transaction states | Govern `REQUEST_RECEIVED`, `RESERVED`, `EVIDENCE_VALIDATED`, `RECEIPT_PREPARED`, `COMMITTED`, and `RESPONSE_AVAILABLE`, exact response reconstruction, legal transitions, and idempotency. |
| Recovery states | Govern `ABORTED`, `SUPERSEDED`, and `RECOVERED`; signed-chain checkpoint replay; partial-artifact preservation; and prohibition on reusing incomplete state. |
| Historical classification | Bind sequence 332, sequence 678, ten missing-response issuances, reservations, incomplete orchestration, and candidate issuances through append-only records without rewriting history. |
| Path controls | Require held no-follow handles, final NT path, root/volume/file ID, owner/DACL, expected links, ADS rejection, case/alias detection, directory-chain checks, and rename/substitution resistance. |
| Dependency manifests | Bind build/runtime tools, recursive input roots, references, CLR/native modules, search paths, configuration, and per-request load-set checks; prohibit Python and Git in runtime authority. |
| Binary manifests | Bind exact source sets, generated identities, compiler/options/architecture, raw PE, normalized IL, installed file/process identity, DLL load set, and configuration for all roles. |
| Reconciliation | Require independently valid committed candidate/fresh graphs, disjoint provenance, 149 shared cases, complementary nested `POS-005`/`POS-006` outer submissions, and public verification. |
| Public verification | Require complete version-aware offline verification with no private key/running service and deterministic classifications for every retained entry/receipt/transition. |
| Adversarial registry | Require direct authority, expectation, outer-interface, child, parser, upgrade, history/recovery, path, dependency, trace, binary, and reconciliation attacks. |
| Traceability registry | Require forward exact-byte-to-reconciliation rows and reverse source/script/utility/binary/principal/ACL/operation/field/dependency/host-artifact provenance; reject circular or summary-only rows. |
| Upgrade/rollback/revocation/DR | Require separately signed transition records before installation, activated-version state, operation-specific rollback/revocation, ledger-preserving disaster recovery, stopped-service verification, and restart proof. |

## Compatibility and current boundary

Retained v1/v3 bytes remain immutable and receive issuance-time classification plus any later append-only recovery/supersession records. New schemas could begin only at a publicly resolved activation sequence. No rejected candidate becomes authority by migration.

At this corrective source-commit boundary, Unit 2 provisioning and verification code is content-bound and four failed bootstrap states remain preserved nonauthority evidence. The current partial state is limited to a stopped service entry, dedicated roots, one nonexportable upgrade key with SYSTEM ownership and metadata-only Administrator access, and one publicly readable certificate; no policy, ledger, executable, authorization, or running upgrade authority exists. A later Unit 2 evidence commit may record only the separately provisioned upgrade boundary and one future-installation-consideration authorization. No canonical record, terminal installation/activation, terminal-ledger change, receipt, reconciliation, matrix run, deployment, or review disposition follows.
